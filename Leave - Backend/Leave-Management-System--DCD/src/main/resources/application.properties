# ===============================
# Application
# ===============================
spring.application.name=LeaveManagement
server.port=${PORT:8084}
server.address=0.0.0.0

# ===============================
# Database (MongoDB)
# ===============================
spring.data.mongodb.uri=${MONGO_URI}

# ===============================
# Admin Configuration
# ===============================
admin.emails=${ADMIN_EMAILS:centeral.gov.coop@gmail.com,admin@gmail.com}
admin.passwords=${ADMIN_PASSWORDS:coop@123,admin123}
admin.names=${ADMIN_NAMES:Admin 1,Admin 2}

# ===============================
# Mail Configuration (Gmail SMTP)
# ===============================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:centeral.gov.coop@gmail.com}
spring.mail.password=${MAIL_PASSWORD:wplm uxqy fwkb rjjm}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000

# ===============================
# Application Custom Config
# ===============================
app.base-url=${APP_BASE_URL:http://localhost:5173}
app.frontend-url=${FRONTEND_URL:http://194.233.84.108:8084}
app.company-name=DCD

# ===============================
# SMS / Twilio Configuration
# ===============================
sms.enabled=${SMS_ENABLED:true}
twilio.enabled=${TWILIO_ENABLED:true}
twilio.account.sid=${TWILIO_ACCOUNT_SID}
twilio.auth.token=${TWILIO_AUTH_TOKEN}
twilio.phone.number=${TWILIO_PHONE_NUMBER}

# Legacy format (if needed by your code)
sms.api.url=https://api.twilio.com/2010-04-01/Accounts/${TWILIO_ACCOUNT_SID}/Messages.json
sms.api.key=${TWILIO_AUTH_TOKEN}
sms.sender.id=${TWILIO_PHONE_NUMBER}


